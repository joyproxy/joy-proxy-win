[package]
name = "joyproxy_relay"
version = "0.1.0"
edition = "2021"
license = "GPL-3.0-or-later"
description = "JoyProxy TCP relay over SOCKS5/HTTP CONNECT"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
base64 = "0.22"
thiserror = "2.0"

[dev-dependencies]
once_cell = "1.19"
